#pragma once

// Hàm tính tổng
double Tong(double a, double b);

// Hàm tính hiệu
double Hieu(double a, double b);

// Hàm tính tích
double Tich(double a, double b);

// Hàm tính thương, trả về false nếu chia cho 0
bool Thuong(double a, double b, double& ketQua);